// src/environments/environment.prod.ts
// Production environment configuration
// For same-origin deployment (e.g., Angular served behind the same domain as the API)

export const environment = {
  production: true,
  apiUrl: '/api',
};
